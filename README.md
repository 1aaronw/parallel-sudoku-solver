A parallelized approach of solving Suodku using multithreading & a non-parallelized approach to compare speedups to. 
To run either programs simply run 
```
python3 solver-parallel.py 
```
Or
```
python3 solver-normal.py 
```
